import React from "react";
import styles from "./modulestyle.module.css";

const CSSModule = () => {
  //return <div className={'${styles.largefont} ${styles.yellowcolor}'}>
  return <div className={"${styles.yellowcolor}"}>CSS Module Style</div>;
};

export default CSSModule;
